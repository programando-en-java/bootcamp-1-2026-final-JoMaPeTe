global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'flight-services'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: [ 'host.docker.internal:8080' ]
  # Usamos host.docker.internal para acceder al servicio desde el contenedor

